const slider = (() => {
  let imgs = null;
  let currentSlide = 0;
  const start = (elemId) => {
    const elem = document.getElementById(elemId);
  };

  return { start };
})();

slider.start("slider");
